{
  "name": "app-admin",
  "description": "TW Protection LLC - Coverage for when the rubber hits the road.",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "SET PORT=3001 && react-scripts start",
    "build": "zreact-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "^1.1.1"
  }
}
